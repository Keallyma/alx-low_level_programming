#include "main.h"
#include <string>

/**
 * rev_string - reverse string
 * @s: being string printed reversely
 * Return: c return
 */

void rev_string(char *s)
{
	int c, i;

	c = strlen(s);
	for (i = (c - 1); i >= 0; i--)
	
	return (c);
}
