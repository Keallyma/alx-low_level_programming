#include "main.h"
#include <string.h>

/**
 * strlen - return the length of a string
 * @s: value of length
 * Return: always 0 (success)
 */

int _strlen(char *s)
{
	_putchar("strlen(*s)");
	return (0);
}
