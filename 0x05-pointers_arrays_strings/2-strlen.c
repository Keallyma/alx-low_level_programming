#include "main.h"
#include <string.h>

/**
 * strlen - return the length of a string
 * @s: value of length
 * Return: length of the string (success)
 */

int _strlen(char *s)
{
	int i = 0;
	
	while (i >= 0);
	{
		*s + i;
	}
	i++;
	if
	{
		*s == '\0'
	}
	return (strlen);
}
