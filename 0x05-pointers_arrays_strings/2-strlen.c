#include "main.h"

/**
 * strlen - return the length of a string
 * @s: value of length
 * Return: always 0 (success)
 */

int _strlen(char *s)
{
	_putchar("strlen(*s) : %d\n", strlen);
	return (0);
}
