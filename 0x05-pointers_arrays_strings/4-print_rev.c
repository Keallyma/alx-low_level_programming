#include "main.h"

/**
 * print_rev - print string in reverse, followed by a new line
 * @s: string being input
 * Return: no return
 */

void print_rev(char *s)
{
	int c = 0; *s;
	
	while (c <= 0; c = s - 2; c--);
	{
	if (*s[c] != '\0')
	{
		_putchar(*s[c]);
	}
	_putchar('\n');
}
