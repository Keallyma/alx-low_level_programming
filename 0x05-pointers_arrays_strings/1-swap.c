#include "main.h"
#include <stdio.h>

/**
 * swap_int - should swap the values of two integers
 * @a: first value of integer received
 * @b: second value of integer received
 * return: no return
 */

void swap_int(int *a, int *b)
{
	*a = *b
	*b = *a
}
